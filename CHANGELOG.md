# Changelog

## [v9.5.38] - 2026-01-27 (Omni-Append Edition)
### 🚀 全源追加引擎 (Omni-Append Engine)
- **体验统一 (Unified Experience)**: 彻底消除“创建”与“维护”的能力落差。现在，追加知识库时也能享受到完整的 5 大采集源（上传/扫描/粘贴/网页/数据库）与暂存区机制。
- **追加暂存区 (Append Staging Area)**: 引入独立的追加缓冲区。采集的材料不会立即入库，而是先进入暂存区等待检阅，确保“所见即所得”。
- **原子化提交 (Atomic Commit)**: 实现了“索引优先、归档殿后”的原子提交逻辑，并自动修正 `manifest.json` 路径，确保系统稳健。

### 🔧 高级选项复用架构 (Unified Advanced Options)
- **DRY 原则**: 将 OCR、元数据、摘要等高级处理逻辑封装为 `render_advanced_options` 统一组件。
- **智能安全锁**: 在追加模式下，系统智能管理“强制重建索引”选项：
    - **默认关闭**: 保护现有索引，仅执行增量追加。
    - **支持重构**: 若用户主动勾选，系统将触发“全量重构”流程（合并新旧文件 -> 清空索引 -> 重新构建），满足深度维护需求。

### 🛠️ 架构重构
- **全能采集组件**: 新增 `src/ui/unified_ingestion.py`，接管所有资料摄入 UI。
- **逻辑解耦**: `src/apppro.py` 瘦身成功，不再包含冗余的上传逻辑。

## [v9.5.37] - 2026-01-26 (Search Revolution Edition)
### 🕷️ 智能搜索技术革命 (Smart Search Breakthrough)
- **Level 0 暴力探测 (Violent Discovery)**: 彻底重构抓取架构，将搜索引擎定义为 L0 种子层。放弃 HTML 解析器，采用正则表达式直接扫描原始 HTTP 字节流，物理绕过 WAF 结构混淆。
- **重定向自动解壳 (Direct Unwrapping)**: 实现了对 Bing (base64)、DuckDuckGo (uddg) 及 Google 跳转链接的物理拆解，直接锁定原始 URL，抓取成功率提升 300%。
- **种子隔离架构**: 搜索引擎结果页不再消耗抓取深度配额，且绝不作为文档落盘，确保知识库纯净度。

### 📦 暂存区治理中心 (Staging Area Inspector)
- **四维交互工具链**: 
    - **📂 预览**: 引入限高滚动 popover，按来源（上传/网页/数据库）分组展示资产，支持一行化显示时间与体积。
    - **📍 定位**: 物理集成 macOS `open -R` 指令，支持一键在 Finder 中打开暂存目录。
    - **🔄 刷新**: 强制重置上传哈希 (Upload Hash) 并重扫描路径，确保全源状态即时对齐。
    - **🧹 清理**: 一键物理粉碎暂存区及关联元数据。
- **审计追踪报告**: 每一份进入暂存区的材料均同步生成 `.meta` 审计文件，但在 IndexBuilder 构建阶段会被 **自动屏蔽**。
- **计数纠偏**: 修复了计数逻辑，严格排除 `.meta` 干扰，确保显示的“已就绪文件”数量 100% 准确。

### 🚀 极致性能与稳定性 (Zero White Screen)
- **Fragment 全闭环**: 彻底移除 Omni 面板内所有的 `st.rerun()`。材料添加、刷新、删除均在局部容器内瞬时完成，交互延迟降至 0ms。
- **自动智能命名**: 引入“材料驱动”的命名算法。添加任意材料后，系统会自动根据暂存区特征填补知识库名称。
- **物理路径自愈**: 在长时抓取任务前后增加目录存在性校验，防止由于 Session 重置导致的 `FileNotFoundError`。

## [v9.5.1] - 2026-01-26 (Omni-Ingestion Unification Edition)
### ⚡ 全源摄入架构统一 (Omni-Ingestion Unification)
- **数据库同步降维**: 彻底移除了独立的“🔌 数据库同步”侧边栏模式，将其能力完整降维融合至“⚡ 全源摄入”面板的 **Tab 5 (数据库快照)**。
- **一切皆源文件**: 实现了数据库查询的物理物料化。无论是整表导出还是自定义 SQL 查询，均会先生成标准的 CSV 文件存入 `task_staging_dir` 暂存区。
- **自定义 SQL 支持**: 新增了“📝 自定义 SQL”模式，支持用户直接编写 SQL 提取特定业务切片数据。
- **流式导出引擎**: 引入 `DatabaseExporter` 核心类，采用 Pandas Chunk 分块读取策略，有效防止 OOM 风险。

### 🛡️ 架构瘦身与清理
- **死代码移除**: 物理删除了 `apppro.py` 中约 200 行冗余的旧版数据库同步 UI 代码。
- **交互归一**: 统一了所有资料摄入的入口逻辑，现在只有唯一的 **“⚡ 全源摄入”** 模式。

## [v9.3.0] - 2026-01-24 (Codebase Governance Edition)
### 🗺️ 代码资产透视系统 (Codebase Cartography)
- **上帝视角索引**: 新增 `scripts/maintenance/audit_codebase.py` 自动化审计工具。
- **僵尸代码猎杀**: 集成 `vulture` 与 `autoflake` 引擎，清理未使用的 Import 引用。
- **文档自动化**: 实现了“代码即文档”的闭环。

### 🛡️ 逻辑修复与死角清理
- **数据库摄入修复**: 修复了 `DBIngestor` 中因 `return` 语句过早导致元数据保存逻辑被截断的 Bug。
- **逻辑断路修复**: 清理了 `config_service` 等模块中所有 `unreachable code`。

## [v9.2.0] - 2026-01-24 (UX/UI Polishing & Fragment Architecture)
### 🚀 交互重构：全域局部渲染架构 (Universal Fragment Architecture)
- **对话区全包裹**: 将核心对话交互区封装为单一 Fragment，实现点击推荐问题时的零闪烁响应。
- **文件管理局部化**: 知识库详情页的文件列表重构为独立 Fragment。
- **直接回复流**: 移除了推荐问题点击后的二次刷新逻辑。

### 🎨 布局与视觉修复
- **登录页侧边栏隔离**: 修复了登录加载阶段因全局 CSS 抢跑导致的侧边栏强制占位问题。
- **上传组件防抖**: 强制锁定了文件上传框的初始高度。

### 🛡️ 功能增强与反馈
- **Finder 深度集成**: 修复了“在 Finder 中显示”和“预览”功能的静默失败问题。
- **上传命名优化**: 修复了手动输入本地路径时的自动命名校验阻断 Bug。

## [v9.1.0] - 2026-01-23 (Flagship Evolution - Performance & Decoupling)
### 🚀 极致性能优化：“零白屏”丝滑体验 (Zero White Screen Optimization)
- **局部刷新革命**: 利用 `st.fragment` 深度重构了工具栏功能开关。
- **无缝对话流**: 移除了回答生成后的强制重载逻辑。
- **调度延迟清零**: 优化了核心调度器，移除了启动任务时的冗余 `st.rerun()`。

### 💬 纯对话模式深度解耦 (Pure Chat Decoupling)
- **知识库零依赖**: 彻底剥离了纯对话模式对底层文件系统的依赖。
- **直连 LLM 流式输出**: 实现了纯对话模式下的直连大模型流式响应链路。

### 🛡️ 稳健性加固与作用域修复
- **变量作用域补全**: 修复了引入 Fragment 隔离后导致的 `UnboundLocalError`。
- **API 冲突消除**: 解决了 `StreamlitAPIException`。
- **文件名安全处理**: 在对话记录导出功能中引入了 `sanitize_filename` 校验。

## [v9.0.0] - 2026-01-22 (Flagship Evolution - Omni-Ingestion)
### 📥 数据摄入革命：从“单选”到“并集”
- **物理暂存区**: 引入了 `task_staging_dir` 缓冲层。
- **即时同步机制**: 实现 `sync_to_staging` 算法。
- **透明化进度**: 新增实时的“待处理暂存区”文件计数器。

### 🛡️ 管理员主权与配额重构
- **配额全面豁免**: 管理员角色实现存储空间配额 100% 豁免。
- **颗粒化配额调节**: 在“账户与访问安全”面板中找回了配额配置功能。

### 🧼 稳健性加固
- **基础函数补全**: 补全了 `save_uploaded_files` 函数。
- **暂存区自愈**: 解决了页面刷新导致暂存目录被误删的问题。
- **日志审计增强**: 强化了终端日志输出。
