# RAG Pro Max 企业级数据主权与资产安全白皮书

**版本**: v9.5.37 (Search Revolution Edition)
**日期**: 2026-01-26

## 1. 数据主权：非必要不推送原则
本项目的核心设计哲学是 **「代码归远程，资产归本地」**。
- **隔离机制**: 所有的向量索引、原始文档、聊天记录、用户密码 Hash 均被严格拦截在 Git 追踪范围之外。
- **意义**: 确保企业在使用 GitHub 等公共托管平台时，即便代码库泄露，其核心知识资产（IP）依然安全地保留在本地服务器。

## 2. 过程主权：全源本地物料化 (Omni-Materialization)
v9.5.1 引入了强制物料化机制，确保**数据绝不流经黑盒**：
- **落地即安**: 无论是网页爬虫数据、数据库快照还是粘贴板文本，必须先作为物理文件（.md/.csv/.txt）存储于本地 `task_staging_dir`。
- **物理并集**: 系统只处理“看得见”的文件。这种设计消除了内存处理的“瞬时黑洞”，让每一次构建都可审计、可备份。

## 3. 审计主权：元数据屏蔽盾 (Meta-Shielding)
v9.5.37 确立了“审计与业务分离”的原则：
- **伴生记录**: 系统为每个文件生成 `.meta` 审计文件，记录来源 IP、采集时间与方式。
- **严格屏蔽**: 在知识库构建阶段，系统底层强制过滤所有 `.meta` 文件，确保审计数据**绝对不会**进入向量索引，防止“元数据污染”干扰检索准确性。

## 4. 存储主权：管理员配额豁免
- **Admin Immunity**: `admin` 角色自动豁免存储配额限制，保障了最高权限的创作自由。
- **Individual Cabin**: 引入“个体安全调节舱”，支持对不同用户进行 MB 级别的精细化空间授权。

---
*RAG Pro Max: 您的数据，永远是您的。*