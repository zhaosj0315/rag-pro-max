# 🛡️ 本地资产保护准则 (Local Assets Protection Doctrine)

**版本:** 1.0
**生效日期:** 2026-01-21

## 核心原则
本项目遵循“代码归远程，资产归本地”的原则。以下定义的目录属于**不可侵犯的本地资产**。

## 1. 禁止清理与删除的路径
即使在进行代码回退、环境清理（如 `git clean`）或系统迁移时，**严禁**删除以下目录及其内容：
- `vector_db_storage/` : 所有的知识库向量索引、元数据及处理后的文本。
- `chat_histories/` : 用户的所有历史对话数据。
- `temp_uploads/` : 原始文件、批量上传包及爬虫中间结果。
- `RAG_PRO_MAX_WHITEPAPER_*.md` : 项目核心里程碑宣传白皮书。
- `ADR_*.md` : 核心架构决策记录。
- `DATA_SOVEREIGNTY_WHITEPAPER.md` : 数据安全主权规范。
- `PROJECT_AUDIT_STANDARD.md` : 全量审计作业标准。
- `UI_UX_DESIGN_LANGUAGE.md` : 交互设计哲学指南。
- `PERFORMANCE_ACCELERATION_WHITEPAPER.md` : 性能与硬件加速白皮书。
- `ROBUSTNESS_SELF_HEALING_SPEC.md` : 系统鲁棒性与自愈规范。
- `config/` : 以下核心配置文件：
  - `users.json` (用户账号与权限)
  - `db_connections.json` (数据库连接配置)
  - `kb_sharing.json` (知识库共享状态)
- `hf_cache/` : 本地下载的模型权重缓存。

## 2. 禁止推送原则
以上目录必须在 `.gitignore` 中明确排除。严禁将任何包含用户私有数据、敏感配置或大规模二进制索引的资产推送到 GitHub 等公共/私人远程仓库。

## 3. 操作规范
- **代码回退**：仅对 `src/` 及其它脚本目录执行 `git reset` 或 `git checkout`。
- **环境清理**：
  - **优先使用**：必须优先使用项目自带的脚本 `scripts/cleanup_materials.sh` 执行例行清理。
  - **禁止使用**：严禁使用 `git clean -fdx` 等未经审计的全局清理命令。
  - **精确清理**：如果需要清理编译缓存，应使用 `find . -name "__pycache__" -exec rm -rf {} +` 等精确命令。
- **误删警示**：任何涉及上述路径的删除操作必须向用户展示受影响的文件夹列表，并获得明确的授权。

## 4. 维护与继承
本准则与 `scripts/cleanup_materials.sh` 互为补充。任何对清理脚本的修改都必须遵循“资产受限”原则，不得将核心资产目录列入删除清单。

---
*注：本准则作为系统最高优先级规则，所有自动化 Agent 和开发者必须严格遵守。*
\n# 🛡️ Codebase Cartography System\nscripts/maintenance/\ndocs/CODE_INDEX.md\ndocs/DEPENDENCY_GRAPH.svg\ndocs/DEAD_CODE_REPORT.txt\ndocs/MISSING_DOCS.json
