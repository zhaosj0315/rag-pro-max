# 用户登录系统使用说明

## 功能概述

已为 RAG Pro Max 添加了完整的用户登录系统，包含以下功能：

### ✨ 核心功能
- **用户注册**: 支持用户名、密码、邮箱注册
- **用户登录**: 安全的密码验证和会话管理
- **演示账号**: 一键使用演示账号（用户名: demo, 密码: demo123）
- **密码修改**: 用户可以修改自己的密码
- **会话管理**: 自动维护登录状态
- **用户信息**: 显示注册时间、最后登录时间等

### 🔒 安全特性
- 密码使用 SHA256 哈希存储
- 会话状态管理
- 用户状态控制（启用/禁用）

## 使用方法

### 首次启动
1. 运行应用：`streamlit run src/apppro.py`
2. 系统会自动显示登录页面
3. 选择注册新账号或使用演示账号

### 演示账号
- 用户名: `demo`
- 密码: `demo123`
- 点击"👤 演示账号"按钮即可快速登录

### 注册新用户
1. 切换到"📝 注册"标签页
2. 填写用户名（3-20个字符）
3. 填写密码（至少6位）
4. 确认密码
5. 邮箱可选填写

### 用户管理
- 在侧边栏可以看到当前登录用户
- 支持修改密码
- 支持退出登录
- 显示用户信息（注册时间、最后登录等）

## 文件结构

```
src/
├── services/
│   └── auth_service.py          # 认证服务核心
├── ui/
│   ├── auth_ui.py              # 登录界面组件
│   └── user_management_ui.py   # 用户管理界面
└── apppro.py                   # 主应用（已添加认证检查）

data/
├── users.json                  # 用户数据存储
└── sessions.json              # 会话数据（预留）
```

## 技术实现

### 认证流程
1. 用户访问应用 → 检查登录状态
2. 未登录 → 显示登录页面
3. 登录成功 → 设置 session_state
4. 进入主应用 → 显示用户菜单

### 数据存储
- 用户数据存储在 `data/users.json`
- 密码使用 SHA256 哈希
- 支持用户状态管理

### 会话管理
- 使用 Streamlit session_state
- 登录状态持久化
- 自动登出功能

## 扩展功能

### 管理员功能
- 创建用户名为 `admin` 的账号可访问用户管理界面
- 支持启用/禁用用户
- 查看用户统计信息

### 未来扩展
- 角色权限管理
- 用户组功能
- 操作日志记录
- 密码强度要求
- 邮箱验证
- 忘记密码功能

## 注意事项

1. **数据安全**: 用户数据存储在本地 JSON 文件中，生产环境建议使用数据库
2. **密码安全**: 当前使用 SHA256，生产环境建议使用更强的哈希算法（如 bcrypt）
3. **会话管理**: 基于 Streamlit session_state，浏览器关闭后需重新登录
4. **备份**: 建议定期备份 `data/users.json` 文件

## 兼容性

- ✅ 完全兼容现有功能
- ✅ 不影响知识库和对话功能
- ✅ 保持原有界面布局
- ✅ 支持所有现有配置

登录系统已无缝集成到现有应用中，用户登录后可正常使用所有 RAG Pro Max 功能。
