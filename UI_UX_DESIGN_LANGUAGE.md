# RAG Pro Max UI/UX 设计语言指南 (Minimalist Unified Design)

**版本**: v9.5.1
**核心哲学**: 归一化、去冗余、心智直觉

## 1. 全源归一化入口 (Omni-Source Unification)
为了解决用户在“面对复杂选项时的决策瘫痪”，我们将数据摄入收敛为唯一的 **“⚡ 全源摄入”** 模式：
- **单一入口**: 废弃顶层导航中的独立“文件”、“互联网”、“数据库”模式。
- **Tab 切换**: 所有的资料来源（文件、目录、粘贴、网页、数据库）均平铺为 Omni 面板内的 5 个标准 Tab。
- **物理统一**: 无论来源何处，所有数据最终都表现为暂存区 (`task_staging_dir`) 中的物理文件，给予用户一致的“实体感”。

## 2. 交互逻辑与视觉规范 (Interaction & Visuals)
- **表单化单行对齐 (Form-like Alignment)**: 为了消除视觉凌乱感，所有的输入组件均遵循 `[标签:] [组件]` 的水平排列模式。
    - 标签宽度锁定为 `0.6` 比例，确保垂直方向上的“冒号对齐”。
- **极致空间压缩 (Spatial Efficiency)**: 
    - **上传组件**: 通过自定义 CSS 移除默认图标与占位符，压缩至传统按钮高度。
    - **小问号提示 (❓)**: 取代冗余的静态说明文字，采用悬浮 Tooltip 承载复杂格式指引。
- **失焦即保存 (Focus-Out Persistence)**: 在粘贴文本等场景，取消“保存”按钮，采用失焦自动持久化。

## 3. 高级选项隐藏原则 (Progressive Disclosure)
- 默认仅展示核心路径。
- 将“OCR、摘要、元数据”等高阶功能置于折叠面板中，保持界面的呼吸感。

## 4. 零白屏与局部响应 (Zero White Screen Standard)
为了消除 Streamlit 架构固有的“刷新迟滞感”，系统确立了极致流畅标准：
- **Fragment 隔离原则**: 所有的功能开关、参数微调、材料收集（上传/路径/粘贴/数据库快照）必须使用 `@st.fragment` 进行生命周期隔离。严禁因局部组件的状态变更触发整个页面的白屏闪烁。
- **无感状态同步**: 在 Fragment 内部，优先使用 `st.empty()` 占位符或原地更新逻辑，避免显式调用 `st.rerun()` 导致的视觉中断。
- **对话流无缝衔接**: 对话引擎严禁在流式输出结束时使用全页刷新。必须通过原地增量渲染“追问建议”，确保用户在多轮对话中感知不到页面的重载。
