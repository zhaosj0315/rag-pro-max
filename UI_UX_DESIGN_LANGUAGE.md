# RAG Pro Max UI/UX 设计语言指南 (Minimalist Unified Design)

**版本**: v9.5.37
**核心哲学**: 归一化、去冗余、心智直觉

## 1. 暂存区治理中心 (Staging Inspector UI)
v9.5.37 确立了物理暂存区的可视化标准：
- **四维工具栏**: 采用 `[预览] [定位] [刷新] [清理]` 的紧凑水平布局，置于 Omni 面板顶部。
- **Popover 预览**: 弃用传统的 Expander，改用 `st.popover` 实现轻量级资产检视。
- **限高滚动 (Scrollable)**: 资产列表必须包裹在 `max-height: 450px` 的 CSS 容器中，防止文件过多撑破布局。
- **来源分组 (Source Grouping)**: 必须按 `Source` 元数据对文件进行视觉分组（如 📂 上传、🌐 网页、🗄️ 数据库）。

## 2. 全源归一化入口 (Omni-Source Unification)
- **单一入口**: 废弃顶层导航中的独立“文件”、“互联网”、“数据库”模式，统一为 **“⚡ 全源摄入”**。
- **Tab 切换**: 所有的资料来源平铺为 Omni 面板内的 5 个标准 Tab。
- **物理统一**: 无论来源何处，所有数据最终都表现为暂存区 (`task_staging_dir`) 中的物理文件。

## 3. 原生溯源交互 (Native Preview Interaction)
- **Eye Icon**: 在文件列表中使用 `👁️` 图标代表“预览”操作。
- **系统级反馈**: 点击预览不应触发 Streamlit 页面重载，而是通过后台 `subprocess` 唤起 OS 级窗口。
- **自动置顶**: 必须配合 AppleScript 逻辑，确保预览窗口自动获得焦点。

## 4. 零白屏与局部响应 (Zero White Screen Standard)
- **Fragment 隔离原则**: 所有的功能开关、参数微调、材料收集必须使用 `@st.fragment` 进行生命周期隔离。
- **无感状态同步**: 在 Fragment 内部，优先使用 `st.empty()` 占位符或原地更新逻辑。
- **对话流无缝衔接**: 对话引擎严禁在流式输出结束时使用全页刷新。

## 5. 视觉规范细节
- **标签宽度**: 锁定为 `0.6` 比例，确保垂直方向上的“冒号对齐”。
- **上传组件**: 通过自定义 CSS 移除默认图标与占位符，压缩至传统按钮高度。
- **状态徽章**: 在知识库详情页顶部使用彩色 Badge (如 🟢 已索引) 替代纯文本状态。