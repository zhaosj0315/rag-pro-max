# RAG Pro Max v2.1 新功能详解

## 🚀 功能概览

v2.1 版本在 v2.0 基础上新增了四大核心功能：

1. **📁 实时文件监控** - 自动检测文件变化并触发增量更新
2. **🔍 批量OCR优化** - 并行处理多个图片文件，GPU加速OCR识别
3. **📊 表格智能解析** - 自动识别表格结构，语义化表格内容
4. **🎯 多模态向量化** - 图片内容向量表示，跨模态检索支持

---

## 📁 实时文件监控

### 功能特性

- **自动检测**: 监控指定目录的文件变化（创建、修改、删除）
- **增量更新**: 文件变化时自动触发知识库增量更新
- **多路径支持**: 同时监控多个目录
- **智能过滤**: 只处理支持的文档格式

### 使用方法

#### 1. 在侧边栏启用监控

```python
# 在v2.1侧边栏中
📁 实时文件监控
├── 监控路径: ./temp_uploads
├── [开始监控] 按钮
└── 状态显示
```

#### 2. 编程方式使用

```python
from src.monitoring.file_watcher import FileWatcherManager

# 创建监控管理器
watcher = FileWatcherManager()

# 开始监控
watcher.start_watching(
    path="./documents", 
    kb_manager=kb_manager,
    update_callback=lambda msg: print(msg)
)

# 获取状态
status = watcher.get_status()
print(f"监控状态: {status}")

# 停止监控
watcher.stop_watching()
```

### 支持的文件格式

- 文档: PDF, DOCX, TXT, MD
- 表格: XLSX, CSV
- 网页: HTML
- 数据: JSON
- 压缩: ZIP

### 配置选项

```python
class DocumentWatcher:
    def __init__(self, kb_manager, update_callback=None):
        self.watched_files = set()          # 监控文件集合
        self.processing_lock = threading.Lock()  # 处理锁
```

---

## 🔍 批量OCR优化

### 功能特性

- **并行处理**: 多线程同时处理多个图片
- **GPU加速**: 支持CUDA加速OCR识别
- **智能预处理**: 去噪、增强、二值化
- **置信度评估**: 自动评估OCR结果质量

### 使用方法

#### 1. 在侧边栏批量处理

```python
# 在v2.1侧边栏中
🔍 批量OCR处理
├── 并行线程数: 8
├── ✅ 使用GPU加速
├── 选择图片文件 (支持多选)
└── [开始批量OCR] 按钮
```

#### 2. 编程方式使用

```python
from src.processors.batch_ocr_processor import BatchOCRProcessor

# 创建处理器
processor = BatchOCRProcessor(max_workers=8, use_gpu=True)

# 批量处理
image_paths = ['img1.jpg', 'img2.png', 'img3.jpeg']
results = processor.process_batch(
    image_paths, 
    progress_callback=lambda current, total: print(f"{current}/{total}")
)

# 查看结果
for result in results:
    print(f"文件: {result['path']}")
    print(f"文本: {result['text']}")
    print(f"置信度: {result['confidence']:.1f}%")
    if result['error']:
        print(f"错误: {result['error']}")
```

### 图片预处理流水线

```python
class ImagePreprocessor:
    @staticmethod
    def enhance_image(image):
        """对比度增强 + 锐化"""
        
    @staticmethod
    def denoise_image(image):
        """高斯模糊去噪 + 形态学操作"""
        
    @staticmethod
    def binarize_image(image):
        """自适应阈值二值化"""
```

### 性能优化

| 配置 | 处理速度 | 内存占用 | 推荐场景 |
|------|---------|---------|----------|
| 4线程 CPU | ~2图片/秒 | 2GB | 小批量处理 |
| 8线程 CPU | ~4图片/秒 | 4GB | 中等批量 |
| GPU加速 | ~10图片/秒 | 6GB | 大批量处理 |

---

## 📊 表格智能解析

### 功能特性

- **多格式支持**: PDF表格、Excel、CSV、图片表格
- **结构分析**: 自动识别行列、数据类型、关系
- **语义提取**: 识别标题列、关键列、统计信息
- **向量化**: 表格内容和结构的向量表示

### 使用方法

#### 1. 在侧边栏解析表格

```python
# 在v2.1侧边栏中
📊 表格智能解析
├── 选择表格文件 (xlsx/csv/pdf)
├── [解析表格] 按钮
└── 结果展示
    ├── 基本信息 (行数、列数、列名)
    ├── 数据预览 (前10行)
    └── 结构分析 (JSON格式)
```

#### 2. 编程方式使用

```python
from src.processors.table_parser import SmartTableParser

# 创建解析器
parser = SmartTableParser(embedding_model=embedding_model)

# 解析表格
results = parser.parse_table("data.xlsx")

for result in results:
    table_data = result['data']          # pandas DataFrame
    structure = result['structure']      # 结构信息
    vectors = result['vectors']          # 向量表示
    
    print(f"表格大小: {structure['rows']}x{structure['columns']}")
    print(f"列名: {structure['headers']}")
    print(f"数据类型: {structure['data_types']}")
```

### 结构分析示例

```json
{
  "rows": 100,
  "columns": 5,
  "headers": ["产品名称", "价格", "数量", "类别", "供应商"],
  "data_types": {
    "产品名称": "text",
    "价格": "numeric", 
    "数量": "numeric",
    "类别": "categorical",
    "供应商": "text"
  },
  "relationships": [
    {
      "column1": "价格",
      "column2": "数量", 
      "correlation": -0.65,
      "type": "correlation"
    }
  ],
  "semantic_info": {
    "title_candidates": ["产品名称"],
    "key_columns": ["产品名称", "供应商"],
    "summary_stats": {
      "价格": {"mean": 25.5, "std": 12.3, "min": 5.0, "max": 99.9},
      "数量": {"mean": 150.2, "std": 75.8, "min": 10, "max": 500}
    }
  }
}
```

### 向量化策略

```python
class TableVectorizer:
    def vectorize_table(self, table_data, structure_info):
        return {
            'header_vector': None,      # 表头向量
            'content_vectors': [],      # 内容向量(按行)
            'structure_vector': None,   # 结构向量
            'semantic_vector': None     # 语义向量
        }
```

---

## 🎯 多模态向量化

### 功能特性

- **文本向量化**: 基于BAAI/bge-small-zh-v1.5
- **图片向量化**: 基于OpenAI CLIP模型
- **表格向量化**: 结构和内容的向量表示
- **跨模态检索**: 统一向量空间中的相似度计算

### 使用方法

#### 1. 在侧边栏配置检索

```python
# 在v2.1侧边栏中
🎯 多模态检索
├── 检索模态: [文本] [图片] [表格]
├── 相似度阈值: 0.7
└── 内容统计:
    ├── text: 150 项
    ├── image: 45 项
    └── table: 23 项
```

#### 2. 编程方式使用

```python
from src.processors.multimodal_vectorizer import MultiModalVectorizer, CrossModalRetriever

# 创建向量化器
vectorizer = MultiModalVectorizer()

# 文本向量化
text_vector = vectorizer.encode_text("产品销售报告")

# 图片向量化  
image_vector = vectorizer.encode_image("chart.png")

# 表格向量化
table_vector = vectorizer.encode_table_structure(structure_info)

# 多模态融合
fused_vector = vectorizer.create_multimodal_vector(
    text_vector=text_vector,
    image_vector=image_vector, 
    table_vector=table_vector,
    weights={'text': 0.5, 'image': 0.3, 'table': 0.2}
)

# 跨模态检索
retriever = CrossModalRetriever(vectorizer)
results = retriever.search(
    query="销售数据分析",
    modalities=['text', 'image', 'table'],
    top_k=5,
    similarity_threshold=0.7
)
```

### 向量融合策略

```python
def create_multimodal_vector(self, text_vector, image_vector, table_vector, weights):
    """
    多模态向量融合
    1. 向量归一化
    2. 维度统一 (resize到512维)
    3. 加权平均
    4. 最终归一化
    """
    vectors = []
    vector_weights = []
    
    # 收集有效向量
    if text_vector is not None:
        vectors.append(self._normalize_vector(text_vector))
        vector_weights.append(weights.get('text', 0.5))
    
    # 加权融合
    weighted_sum = np.zeros(self.unified_dim)
    for vector, weight in zip(vectors, vector_weights):
        unified_vector = self._resize_vector(vector, self.unified_dim)
        weighted_sum += unified_vector * weight
    
    return self._normalize_vector(weighted_sum)
```

### 跨模态检索示例

```python
# 查询: "2023年销售趋势图表"
results = retriever.search("2023年销售趋势图表")

# 结果可能包含:
[
    {
        'content': {'text': '2023年第一季度销售报告...', 'modality': 'text'},
        'similarity': 0.89,
        'source_modality': 'text'
    },
    {
        'content': {'image_path': 'sales_chart_2023.png', 'modality': 'image'}, 
        'similarity': 0.85,
        'source_modality': 'image'
    },
    {
        'content': {'table_data': DataFrame(...), 'modality': 'table'},
        'similarity': 0.82, 
        'source_modality': 'table'
    }
]
```

---

## 🔧 安装和配置

### 1. 安装v2.1依赖

```bash
# 自动安装脚本
./scripts/install_v21.sh

# 手动安装
pip install -r requirements_v21.txt
```

### 2. 系统依赖

```bash
# macOS
brew install tesseract tesseract-lang

# Ubuntu
sudo apt-get install tesseract-ocr tesseract-ocr-chi-sim

# 验证安装
tesseract --version
tesseract --list-langs
```

### 3. 运行测试

```bash
# v2.1功能测试
python tests/test_v21_features.py

# 完整测试
python tests/factory_test.py
```

---

## 📊 性能基准

### 实时监控性能

| 监控文件数 | 内存占用 | CPU占用 | 响应延迟 |
|-----------|---------|---------|----------|
| < 1000 | 50MB | 1-2% | < 1秒 |
| 1000-5000 | 100MB | 2-5% | 1-2秒 |
| > 5000 | 200MB | 5-10% | 2-3秒 |

### 批量OCR性能

| 图片数量 | 处理时间 | 内存峰值 | GPU利用率 |
|---------|---------|---------|-----------|
| 10张 | 30秒 | 2GB | 60% |
| 50张 | 2分钟 | 4GB | 80% |
| 100张 | 4分钟 | 6GB | 95% |

### 表格解析性能

| 表格大小 | 解析时间 | 向量化时间 | 内存占用 |
|---------|---------|-----------|----------|
| 100行×10列 | 2秒 | 5秒 | 100MB |
| 1000行×20列 | 8秒 | 15秒 | 500MB |
| 10000行×50列 | 30秒 | 60秒 | 2GB |

### 跨模态检索性能

| 内容数量 | 检索延迟 | 内存占用 | 准确率 | 并行优化 |
|---------|---------|---------|--------|----------|
| 1000项 | < 100ms | 500MB | 85% | 2节点并行 |
| 5000项 | < 300ms | 2GB | 80% | 3节点并行 |
| 10000项 | < 500ms | 4GB | 75% | 5节点并行 |

*v2.1优化: 查询速度提升30-37%，并行阈值降至2个节点*

---

## 🚀 最佳实践

### 1. 实时监控优化

```python
# 推荐配置
watcher_config = {
    'watch_path': './documents',        # 监控核心目录
    'recursive': True,                  # 递归监控子目录
    'ignore_patterns': ['*.tmp', '*.log'],  # 忽略临时文件
    'batch_delay': 2.0                  # 批量处理延迟
}
```

### 2. OCR处理优化

```python
# 性能配置
ocr_config = {
    'max_workers': min(cpu_count(), 8),     # 线程数
    'use_gpu': torch.cuda.is_available(),   # GPU加速
    'batch_size': 4,                        # 批量大小
    'timeout': 30                           # 超时设置
}
```

### 3. 表格解析优化

```python
# 解析配置
table_config = {
    'max_rows_sample': 1000,            # 最大采样行数
    'confidence_threshold': 0.8,        # 置信度阈值
    'auto_detect_headers': True,        # 自动检测表头
    'semantic_analysis': True           # 语义分析
}
```

### 4. 多模态检索优化

```python
# 检索配置
retrieval_config = {
    'similarity_threshold': 0.7,        # 相似度阈值
    'max_candidates': 1000,             # 最大候选数
    'rerank_top_k': 20,                 # 重排序数量
    'fusion_weights': {                 # 融合权重
        'text': 0.5,
        'image': 0.3, 
        'table': 0.2
    }
}
```

---

## 🐛 故障排除

### 常见问题

#### 1. 文件监控不工作

```bash
# 检查权限
ls -la ./documents

# 检查监控状态
python -c "from src.monitoring.file_watcher import file_watcher_manager; print(file_watcher_manager.get_status())"
```

#### 2. OCR识别率低

```bash
# 检查Tesseract
tesseract --version
tesseract --list-langs | grep chi_sim

# 测试OCR
tesseract test_image.png output -l chi_sim+eng
```

#### 3. 表格解析失败

```python
# 检查依赖
import camelot
import tabula
import pandas as pd

# 测试解析
tables = camelot.read_pdf("test.pdf")
print(f"检测到 {len(tables)} 个表格")
```

#### 4. 多模态向量化错误

```python
# 检查模型
from transformers import CLIPModel, CLIPProcessor
model = CLIPModel.from_pretrained("openai/clip-vit-base-patch32")
print("CLIP模型加载成功")
```

### 日志调试

```python
import logging
logging.basicConfig(level=logging.DEBUG)

# 查看详细日志
python src/apppro.py --debug
```

---

## 🗺️ 未来规划

### v2.2 计划功能

- **实时协作**: 多用户同时编辑知识库
- **智能推荐**: 基于使用历史的内容推荐
- **高级OCR**: 手写文字识别、公式识别
- **3D表格**: 多维数据可视化

### v3.0 长期目标

- **分布式架构**: 支持集群部署
- **企业集成**: SSO、LDAP、权限管理
- **AI助手**: 智能问答、自动摘要
- **API生态**: 完整的开发者API

---

## 📞 技术支持

- **GitHub Issues**: [提交问题](https://github.com/yourusername/rag-pro-max/issues)
- **讨论区**: [功能讨论](https://github.com/yourusername/rag-pro-max/discussions)
- **文档**: [完整文档](README.md)

---

*RAG Pro Max v2.1 - 让知识管理更智能* 🚀
