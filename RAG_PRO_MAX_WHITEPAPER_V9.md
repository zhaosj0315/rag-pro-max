# RAG Pro Max 业务逻辑与系统架构白皮书 (v9.5.37)

## 💡 核心业务哲学：一切皆源文件 (Everything is a Source File)

本系统彻底摒弃了传统知识库在摄入端的碎片化模式（API/数据库/文件分离），首创**「物理并集」架构**。无论是动荡的网络爬虫数据，还是高并发的数据库快照，系统均在“暂存区”将其强制物料化为本地物理文件。这一逻辑将外部环境的不确定性转化为本地 I/O 的极度稳定性，为企业构建了绝对可靠的知识壁垒。

---

## 模块一：全源数据归一化 (Data Acquisition & Stabilization)

本模块的业务目标是“应收尽收”与“标准化”，消除数据孤岛。

*   **🗄️ 结构化数据降维 (DB Snapshot):** 将复杂的 SQL 数据库表结构实时“降维”为标准 `.csv` 文件，实现结构化业务数据与非结构化文档在系统层面的“平权与对等”。
*   **🌐 深度网络情报捕获 (Web Discovery):** 采用 Level 0 暴力探测技术，直接从 HTTP 原始流中通过正则提取硬编码链接，无视前端防爬混淆，并将网页“脱水”为高纯度 Markdown，适合监控竞品动态与行业报告。
*   **🧩 碎片化办公协同:** 支持从剪贴板即时接收灵感文本，系统自动赋予其物理文件身份和生命周期；多格式文件上传伴随去重哈希校验，杜绝企业存储资源浪费。
*   **🛡️ 物理隔离与审计:** 每个业务任务分配独立 UUID 空间，所有入库数据均带有 `.meta` 审计标签（记录来源与时间戳），确保数据资产全程可溯源。

---

## 模块二：知识资产配置与加工 (Enrichment & Isolation)

本模块负责定义知识库的“智商”边界与安全边界。

### 1. 企业级多租户安全
*   **特征感知命名:** 算法自动提取数据特征（如域名、核心词）生成库名。
*   **租户物理隔离:** 强制追加 `{用户名}_` 前缀（如 `admin_Google_20260126`），在文件系统层面天然隔绝不同用户间的数据越权风险。

### 2. 智能加工引擎 (按需开启)
*   **全盲区消除 (OCR):** 激活多模态处理器，解构图片及扫描件中的隐藏商业价值。
*   **结构化提纯:** 利用 LLM 自动提取行业分类与高频关键词，生成文档级 AI 摘要，加速后续宏观检索效率。

---

## 模块三：自动化构建流水线 (Construction Pipeline)

当数据与配置就位，系统触发高并发工业级流式构建。流水线执行严格的串行逻辑，确保资产沉淀的原子性：

1.  **语义索引构建:** 生成核心高维向量与索引存储（自动屏蔽无用的审计元数据）。
2.  **原件物理归档 (Raw Sources):** 系统将暂存区有效文件 100% 镜像存档，作为企业“不可篡改”的原始电子凭证。
3.  **影子数仓构建 (Data Core):** 识别到业务表格后，自动构建 SQLite 本地分析库，为后续的数据穿透打下基础。
4.  **模型锁定 (Model Locking):** 将当前使用的 Embedding 模型参数写入底层档案，防止未来模型升级导致的历史知识库检索失效。

---

## 模块四：闭环检索与业务决策 (Retrieval & Decision Loop)

这是面向终端用户的“最后一公里”，重点解决回答的准确率与用户体验。

### 1. 意图严选与路由 (Intent Routing)
*   **Text-to-SQL 分流:** 用户的提问首先接受系统意图研判。涉及数据统计（如“上季度销量总和”）的问题，将直接路由至 `DataAnalystEngine`，通过 SQL 执行精确计算，而非模糊的语义检索。
*   **标准 RAG 分流:** 通用知识问答则进入传统的混合检索（向量 + BM25 关键词增强）通道。

### 2. 深度思考与体验优化
*   **降噪精排 (Rerank):** 对初步检索结果进行交叉编码器二次打分，剔除伪相关内容。
*   **零闪烁流式输出:** 采用打字机式平滑输出，极大降低用户在等待复杂 AI 推理时的焦虑感。

### 3. 原生系统级溯源 (macOS Integration)
*   回答中的每一个引用角标都直接锚定到 `raw_sources` 中的物理原件。
*   点击引用，系统自动唤起 macOS 原生 `qlmanage` 并强制置顶预览窗口，实现**“所见即所得”的桌面级溯源体验**，极大提升了用户对 AI 结果的信任度。