# RAG Pro Max v9.8.0 企业级测试指南

**版本**: v9.8.0 (DA-ECP V4.5 Edition)
**更新日期**: 2026-01-31  
**核心基准**: DA-ECP V4.5、资源治理、全景日志

---

## 🧪 v9.8.0 DA-ECP V4.5 深度理解专项测试

### 1. 结构化解析与造数测试 (Structure & JIT Test)
- **脚本**: `tests/verify_deep_understanding.py`
- **场景**: 模拟上传 "真实数据表" 和 "数据字典文件" 的混合场景。
- **预期**:
    1. **StructureParser**: 正确识别并解析数据字典，不将其作为普通数据入库。
    2. **Micro-Profiling**: 正确提取实体表的 `enums` 和 `stats`。
    3. **Schema Enrichment**: 生成的 `business_schema.json` 中包含完整的表结构和推演出的血缘关系。
- **运行命令**:
    ```bash
    python3 tests/verify_deep_understanding.py
    ```

---

## 🧪 v9.7.0 全域治理专项测试

### 1. 监控体系重构测试 (Monitoring Migration Test)
- **场景 A (普通用户)**: 登录非 Admin 账号，检查侧边栏。
    - **预期**: "📊 监控" 标签页 **不应存在**。
- **场景 B (管理员)**: 登录 Admin 账号，进入 "👤 我的" -> "🛡️ 资源治理"。
    - **预期**: 
        1. 包含 "📊 实时系统监控" 标签页。
        2. 监控面板头部为单行 5 列布局（标题/倒计时/刷新/进度/按钮）。
        3. 底部不再显示重复的进度条。

### 2. 全景日志系统测试 (Panoramic Logs Test)
- **场景**: 在 "资源治理" -> "📜 系统全景日志" 中切换子标签。
- **操作**: 
    1. 点击 "👤 用户行为审计": 应显示结构化的 DataFrame 表格。
    2. 点击 "🖥️ 系统终端日志": 应显示折叠式的 `.jsonl` 或 `.log` 文件列表。
- **预期**: 顶部仪表盘应正确统计 "日志文件数" 和 "物理占用"。

---

## 🧪 v9.5.37 搜索革命专项测试

### 1. 暴力探测引擎测试 (Violent Discovery Test)
- **场景**: 在开启代理或复杂内网环境下进行智能搜索。
- **操作**: 运行 `python scripts/maintenance/debug_search_live.py`。
- **预期**:
    1. 脚本应直接输出 Bing/DDG 的原始 HTML 源码片段。
    2. 正则表达式应成功提取至少 5-10 个 `https://` 链接。
    3. 如果包含重定向链接 (bing.com/ck/...)，应输出 "Unwrapped: https://real-site.com"。

### 2. 原生预览集成测试 (Native Preview Test)
- **场景**: 在知识库详情页点击任意文档的 `👁️` 图标。
- **环境**: macOS Monterey+。
- **预期**:
    1. 系统无报错，Streamlit 界面无白屏。
    2. macOS QuickLook 窗口瞬间弹出。
    3. 窗口自动置顶 (Frontmost)，无需用户手动寻找。

---

## 🧪 v9.5.1 全源摄入与架构测试

### 1. 暂存区审计测试 (Staging Audit)
- **操作**: 上传文件后，点击顶部工具栏的 **📂 查看** 图标。
- **预期**:
    1. 弹出 Popover 窗口，显示文件列表。
    2. 文件列表应包含 `Source: [文件上传]` 或类似标签。
    3. 不应显示 `.meta` 文件（虽然物理存在，但 UI 应过滤或友好展示）。

### 2. 混合源构建测试
- **操作**: 依次执行：上传 1 个 PDF，抓取 1 个网页，导出一张数据库表。点击“🚀 立即创建”。
- **预期**: 构建日志显示 IndexBuilder 成功识别并处理了 PDF、Markdown (网页) 和 CSV (数据库) 三种异构文件，且生成的 `manifest.json` 中包含这三个文件的指纹。

---

## 🏢 企业级测试概述

RAG Pro Max v9.5.37 重点强化了**搜索链路与物理预览**的完整性验证。

### 🧪 核心测试领域
- **Regex Robustness**: 验证正则表达式在不同 HTML 结构下的提取能力。
- **Process Isolation**: 验证在 `ThreadPoolExecutor` 下爬虫任务的稳定性。
- **Variable Visibility**: 验证在 Fragment 局部刷新下，全局调度变量是否可见。

---

## 📊 最新测试结果 (v9.7.0)

### 🏆 总体测试状态
```
✅ 通过: 215/215 模块 (100%)
❌ 失败: 0/215 模块 (0%)
⚠️  跳过: 0/215 模块

🎯 企业级质量标准: ✅ 优秀 (100%)
🔒 权限隔离测试: ✅ 通过 (User/Admin 视图分离)
```

### 📋 核心功能测试结果
| 功能模块 | 测试数量 | 通过率 | 状态 |
|----------|----------|--------|------|
| 🛡️ 资源治理中心 | 12 | 100% | ✅ |
| 📜 全景日志系统 | 8 | 100% | ✅ |
| 📊 极简监控 UI | 5 | 100% | ✅ |
| 🕷️ 暴力链路探测 | 15 | 100% | ✅ |
| 👁️ macOS 原生预览 | 5 | 100% | ✅ |

---

**🎯 目标**: 确保 RAG Pro Max 在引入全域治理架构后，权限管控严密，系统观测性显著提升。