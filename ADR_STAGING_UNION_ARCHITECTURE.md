# ADR: 从“优先级互斥”到“物理暂存并集”的架构演进

**日期**: 2026-01-22
**版本**: v9.0.0 (Flagship Evolution)
**状态**: 已通过 (ACCEPTED)

## 1. 背景 (Context)
在 v8.8 之前的版本中，系统对“文件上传”、“本地路径”、“文本粘贴”的处理采用了基于单一变量 `uploaded_path` 的覆盖逻辑。这种逻辑虽然实现简单，但存在致命缺陷：用户无法在同一个任务中混合使用多种来源，且后加入的来源会无情覆盖掉之前的努力。

## 2. 决策 (Decision)
我们决定彻底摒弃“变量争夺”模式，引入 **「物理暂存区 (Physical Staging Area)」** 架构：
- **Staging Area**: 在系统 `temp_uploads/` 目录下为每个会话创建唯一的物理桶。
- **AND Logic**: 所有的摄入组件不再修改路径，而是向该物理桶投递文件副本。
- **Late Anchoring**: 仅在执行构建动作的前一刻，才将路径锁定为该物理桶。

## 3. 技术权衡 (Consequences)
- **优点**:
    - **并集支持**: 完美支持“文件 + 目录 + 粘贴”的随意组装。
    - **解耦**: 前端 UI 的交互状态与后端构建引擎完全解耦。
    - **自愈性**: 引入路径校验逻辑，解决了 Streamlit 刷新导致的目录丢失风险。
- **挑战**:
    - **IO 开销**: 引入了额外的物理拷贝。权衡后认为，相对于数据安全与交互灵活性，这点开销在企业级场景下可以忽略。

## 4. 结论 (Conclusion)
此架构奠定了 RAG Pro Max 作为“全能数据收集器”的技术基石，为未来引入更多异构数据源（如 API、网盘等）预留了无限的扩展空间。
