# RAG Pro Max v8.8.0 企业用户手册 (Flagship Unified Edition)

**版本**: v8.8.0  
**更新日期**: 2026-01-22  
**核心特性**: 三大全能入口、智能意图识别、全异构数据库集成

---

## 🚀 快速上手：三大全能数据源

系统通过深度整合，提供三个“一站式”资料摄入入口，覆盖本地、互联网与企业系统数据。

1. **📂 文件上传 (静态数据万能收集器)**
   - **多源并行收集 (AND 逻辑)**: 您可以**同时**使用以下三种方式收集材料，系统将自动取其并集：
     - **拖拽上传**: 将电脑上的文件拖入上传区，系统即时搬运至暂存区。
     - **本地路径**: 输入本地目录路径，点击 **`➕ 添加目录`**，系统将递归扫描并同步所有文件。
     - **粘贴文本**: 展开面板粘贴内容，失焦即自动保存为文本文件。
   - **暂存区管理**: 
     - 顶部会实时显示 `📦 待处理暂存区` 的文件总数。
     - 如果操作有误，可点击 **`🧹 清空暂存`** 一键重置。
   - **最终确认**: 当您收集齐所有材料后，点击底部的 **`🚀 立即创建`**，系统将对暂存区内的全量合集进行向量化处理。

2. **🌐 互联网提取 (动态网络智能入口)**
   - **智能识别**: 
     - 输入 **URL** (如 `https://...`) -> 触发 **精准网页爬取**。
     - 输入 **关键词** (如 `AI 行业报告`) -> 触发 **全网智能搜索**。
   - **领域感知**: 可选择“技术”、“医疗”等行业上下文，精准路由搜索引擎。
   - **指数级递归逻辑 (5+25)**: 
     - **Level 0 (种子页)**: 您输入的 URL 不计入配额，仅作为抓取起点。
     - **Level 1**: 系统从种子页提取链接，抓取前 $n$ 个网页。
     - **Level 2**: 从 Level 1 的网页中深度扩散，抓取 $n^2$ 个网页。
     - *示例*: 设定“页数=5”，系统将精准抓取 $1 + 5 + 25 = 31$ 个文档，完美兼顾广度与深度。

3. **🔌 数据库同步 (结构化业务入口)**
   - **连接企业库**: 支持 MySQL, Oracle, PostgreSQL 等 9 种数据库。
   - **可视化选表**: 实时预览表结构与数据采样，一键构建业务知识库。

---

## 🔌 企业级全源数据库集成 (v8.6.9)

在 v8.6.9 中，系统实现了对 9 种主流数据库的全面支持，支持从本地到云端、从常规库到大数据引擎的全量接入。

### 1. 极广适配列表
- **MySQL / PostgreSQL / SQL Server / Oracle**: 企业核心业务库。
- **SQLite / DuckDB**: 本地数据文件与轻量分析。
- **ClickHouse**: 高性能 OLAP 分析。
- **MaxCompute (DataWorks) / Snowflake**: 云原生大数据平台。

### 2. 配置与深度预览
- **入口**: **「🔐 资源治理」** -> **「🔌 数据源连接」**。
- **四维透视**: 点击 **「🔍 预览」** 可查看字段定义、50 行数据采样、外键关系及业务统计信息。

### 3. 一键同步构建 (侧边栏革命)
- **入口**: 侧边栏选择 **「🔌 数据库同步」**。
- **即时预览**: 选表时点击 **`👁️`** 即可在气泡中即时预览数据，无需切换页面。
- **全选同步**: 点击“全选所有表”并输入知识库名称，点击底部的 **`🚀 立即创建`** 即可开始。系统会自动完成镜像拉取与知识库构建。

---

## ⚡ 智能问答联动 (Smart Linkage)

- **自动感应**: 系统会自动识别库的能力。进入数据库知识库时，**“数据分析”** 开关将自动为您开启。
- **统一命名**: 点击知识库名称旁的 **💡** 按钮，系统会结合当前模式为您提供智能起名建议。

---

## 🕸️ 智能图谱构建器 (v8.2.0)

在 v8.2.0 中，系统学会了像人类专家一样“读懂”您的数据结构。

### 1. 物理特征画像
- **自动识别**: 构建知识库时，系统会自动扫描数据表，找出哪个字段是主键（唯一标识），哪些字段是分类枚举（如“状态”）。
- **收益**: 当您问“有哪些状态？”时，系统能秒级从缓存中调取答案，而无需耗时的全表扫描。

### 2. 血缘关系推演
- **关联发现**: 系统会自动分析表与表之间的关系（例如：`orders.user_id` 和 `users.id` 是关联的）。
- **复杂查询**: 这使得系统能轻松应对“查询北京用户的平均订单金额”这类跨表难题，准确率大幅提升。

---

## 🧬 按需激活的“双核”力量 (v8.1.2 增强)

通过“铁闸门”逻辑与内核加固，系统将提供更精准的数据洞察。

### 1. 意图严选：听话的“专家”
- **默认状态**: 默认开启 **RAG 模式**（博学助手模式），检索文档原文。
- **授权激活**: 只有当您在对话框上方手动打开 **「数据分析」** 开关时，系统才会启动 SQL 推演。

### 2. 逻辑内核加固
- **原子化拆解**: 面对复杂问题，系统会像专家一样分步骤拆解（如：先筛选范围，再关联数据，最后计算指标），拒绝逻辑跳跃。
- **精准选表**: 在多张表存在同名字段（如“金额”）时，系统能根据您的问题自动区分是查“销售表”还是“退款表”。

### 3. 全链路审计 (v8.1.3)
- **配置透明**: 每次构建知识库，系统都会在后台日志中留存一份“配置清单”，明确记录您当时是否开启了 OCR 或数据分析功能，方便事后核查。
- **思维双写**: AI 的所有思考过程（选表理由、拆解逻辑）不仅在屏幕上显示，也会完整写入日志文件，实现 100% 可追溯。

---

## 🕷️ 饱和式网页抓取 (Crawler Saturation)
- **50,000 页上限**: 现已支持单次抓取最多 5 万个网页，完美适配大型文档中心搬运。
- **自动对齐**: 抓取后的网页表格如果开启了「智能数据分析」，将直接支持 SQL 统计。

---

## 🛡️ 账户与权限管理
- **存储配额**: 系统为普通用户设定了存储上限（默认 100MB）。
- **管理员特权**: `admin` 角色自动豁免配额限制（无限空间）。
- **动态调节**: 管理员可在「我的 -> 账户与访问安全」中，勾选用户并使用「个体安全调节舱」手动修改其配额（单位：MB，设为 -1 表示无限）。
- **实时审计**: 管理员可以通过后台日志看到 `[Route Discovery]` 信号，了解系统当前为何选择了 RAG 或 SQL 模式。
