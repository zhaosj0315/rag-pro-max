# RAG Pro Max v9.5.37 企业用户手册 (Search Revolution)

**版本**: v9.5.37  
**更新日期**: 2026-01-26  
**核心特性**: 暴力抓取引擎、重定向解壳、暂存区治理中心

---

## 📂 暂存区精密管理 (Staging Inspector)

在“全源摄入”顶栏，系统为您提供了四位一体的交互工具链：

1.  **📂 资产预览**: 
    - 点击图标弹出 **Staging Popover** 面板。
    - 自动按 **采集方式** 分组展示（如：[网页抓取]、[文本粘贴]）。
    - 每一行展示 `文件名 (大小 | 采集时间)`，.meta 审计文件对用户不可见，但在后台确保了数据的可追溯性。
2.  **📍 Finder 定位**: 
    - 点击定位图标，系统将直接在 macOS Finder 中打开当前的物理暂存文件夹，方便您进行文件级审计。
3.  **🔄 同步刷新**: 
    - **强制重置**: 此按钮不仅刷新 UI，还会重置内部的 **上传哈希 (Upload Hash)**。
    - **场景**: 当您发现修改了本地文件但系统未检测到变化，或想重新上传同名文件时，请使用此功能。
4.  **🧹 彻底清空**: 
    - 一键物理粉碎暂存区所有材料及 .meta 文件，重置构建任务。

---

## 🌐 智能搜索革命 (Smart Search)

在 v9.5.37 中，搜索模式不再是“碰运气”，而是一套精密确定的算法：

1.  **Level 0 探测**: 系统会先从搜索引擎（Bing/DDG）探测初始链接。探测过程中，终端会输出 `锁定 X 个高价值目标`。
2.  **自动解壳**: 系统会自动从复杂的跳转链接中剥离出原始网址，实现直连抓取，成功率提升至 100%。
3.  **暴力探测**: 如果网页结构复杂，系统会自动切换至“暴力正则模式”，从代码流中强行打捞知识。
4.  **中英双语**: 输入中文关键词，系统会自动匹配英文语境，确保获取全球维度的知识。

---

## 🚀 全源摄入：一切皆源文件 (Omni-Ingestion)

系统通过唯一的 **“⚡ 全源摄入”** 入口，提供五个核心数据采集维度。所有材料均会自动触发 **自动命名** 逻辑。

### 1. 📂 文件上传
- **零白屏交互**: 采用 Streamlit Fragment 技术，文件添加与删除均为瞬时响应。
- **批量处理**: 自动去重，实时显示文件计数。

### 2. 📁 扫描路径
- **递归同步**: 输入本地文件夹路径，系统会自动递归扫描并镜像所有有效文件。
- **元数据审计**: 同步生成 .meta 文件，记录原始路径来源。

### 3. 📝 文本粘贴
- **即时捕捉**: 适用于保存临时会议纪要、代码片段。
- **物理化**: 内容将被保存为 `Pasted_时间戳.txt`，拥有独立的文件生命周期。

### 4. 🌐 网页摄入
- **智能路由**: 输入 URL 自动启动爬虫，输入关键词自动启动全网搜索。
- **Markdown 转化**: 所有网页均会被转化为纯净的 Markdown 格式。

### 5. 🗄️ 数据库快照
- **归一化**: 无论是整表导出还是自定义 SQL，最终都将以 CSV 文件的形式存在于暂存区。
- **流式导出**: 内置防爆内存引擎，轻松应对百万级行数的导出任务。

---

## 🚀 增强追加 (Omni-Append)

在 v9.5.38 中，我们在知识库详情页引入了 **“全源追加引擎”**，彻底终结了“只能上传文件”的简陋追加时代。

1.  **进入追加模式**: 在知识库详情页，展开 **“🚀 增强追加 (Omni-Append)”** 面板。
2.  **采集暂存**: 使用与创建时完全一致的 5 大采集器，将文件抓取或导入至 **追加暂存区**。您可以在提交前随意检阅、删除。
3.  **高级选项**: 勾选 OCR、元数据或摘要选项（注意：追加模式下系统自动禁用“强制重建索引”，保护您的核心资产）。
4.  **确认提交**: 点击 **“🚀 确认追加”** 按钮，系统将自动执行：
    - **增量索引构建**: 仅对暂存区的新文件进行向量化。
    - **文件物理归档**: 成功后自动移动至 `raw_sources`。
    - **路径自动修正**: 智能修正清单引用，确保预览功能正常。

---

## 👁️ 原生溯源体验 (Native Preview)

在知识库详情页的文件列表中，点击 **`👁️`** 图标：
- **系统级调用**: 直接唤起 macOS 的 `qlmanage` 预览工具。
- **自动置顶**: 系统会自动执行脚本将预览窗口拉至最前端。
- **所见即所得**: 您看到的就是 `raw_sources/` 目录下的物理原件。

---

## 🕸️ 智能图谱构建器

### 1. 物理特征画像
- **自动识别**: 构建知识库时，系统会自动扫描数据表，找出哪个字段是主键，哪些字段是分类枚举。

### 2. 血缘关系推演
- **关联发现**: 系统会自动分析表与表之间的关系（例如：`orders.user_id` 和 `users.id`）。

---

## 🛡️ 账户与权限管理
- **存储配额**: 系统为普通用户设定了存储上限（默认 100MB）。
- **管理员特权**: `admin` 角色自动豁免配额限制。
- **动态调节**: 管理员可在「我的 -> 账户与访问安全」中，使用「个体安全调节舱」手动修改其配额。
