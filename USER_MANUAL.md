# RAG Pro Max v9.9.0 用户手册 (Pure Chat Multi-Session Edition)

**版本**: v9.9.0  
**更新日期**: 2026-02-03  
**核心特性**: 纯对话多会话隔离、DA-ECP V4.5、微观画像、结构化语义理解、全域治理

---

## 🧠 数据分析增强构建协议 (DA-ECP V4.5)

RAG Pro Max v9.8.0 引入了全新的构建协议，旨在让系统在 "读过" 您的数据之前，先 "读懂" 您的业务。

### 1. 核心理念：构建即理解
传统的 RAG 系统只在构建时做文本索引，而 v9.8.0 引入了 **"语义建模"** 环节：
- **实体数据 (Solid Tables)**: 系统会对真实的 Excel/CSV 数据表进行 **微观画像 (Micro-Profiling)**，自动统计枚举值、数值范围和空值分布。
- **虚拟表定义 (Virtual Tables)**: 您现在可以直接上传 **数据字典** 或 **表结构定义文档**。系统会自动识别这些文件，从中提取表结构和字段含义，构建出一张 "逻辑蓝图"，而无需导入一行真实数据。

### 2. 终端全息日志 (Terminal Holographic Logs)
构建知识库时，请留意终端输出的全新图标系统，它们代表了系统对您数据的深度思考：

- **🔍 [嗅探]**: 系统正在分析文件的物理特征，判断它是真实数据还是结构定义。
- **📦 [实体数据] -> 全息搬运工**: 判定为真实数据表，启动画像分析。
- **📐 [结构定义] -> 建筑师模式**: 判定为数据字典，启动结构解析，只提取逻辑，不存数据。
- **✅ [语义建模]**: 系统已完成对业务逻辑的固化，建立了表与表之间的血缘关系图谱。

### 3. 最佳实践
- **混合构建**: 推荐将 "真实业务数据" 与 "数据字典文档" 同时投喂给系统。DA-ECP 协议会自动将两者结合，利用字典来解释真实数据的含义，显著提升 Text-to-SQL 的准确率。

---

## 🛡️ 全域治理与监控中心 (Governance & Monitoring)

**注意**: 本部分功能仅对拥有 Admin 权限的管理员开放。

### 1. 资源治理中心
我们将原有的散落在主页侧边栏的 **"📊 监控"** 与 **"⚙️ 智能调度"** 功能，彻底降维收纳至后台：

- **入口**: 侧边栏 -> **👤 我的** -> **🛡️ 资源治理 (Admin Only)**
- **标签页矩阵**:
    - **🛡️ 资源深度治理**: 上帝视角管理所有知识库资产。
    - **📊 实时系统监控**: 查看 CPU/内存负载、系统响应时间及 5秒级实时倒计时。
    - **⚙️ 智能调度**: 调节系统并发参数与资源配额。
    - **📜 系统全景日志**: 深度融合了“用户行为审计”与“底层终端日志”，支持双视图切换。

### 2. 监控系统重构
- **极简 UI**: 监控面板头部现在采用单行 5 列布局，一屏掌握所有关键指标。
- **趋势分析**: 历史趋势图表默认自动展开，无需额外点击。

---

## 🚀 提问艺术：精准提问 (Precise Query)

在侧边栏聊天控制区，您会看到一个新的开关：**「精准提问」**。

- **功能原理**: 开启后，系统不会直接将您的问题丢给模型，而是先由一个“意图分析师”对您的问题进行扩充。
- **适用场景**: 当您的提问非常简短（如“对比两份报告”）时，开启此功能，系统会自动找出“哪些报告”、“对比哪些维度”，从而获得极高质的回报。
- **注意**: 开启此功能会增加约 1-2 秒的预处理延迟，但答案深度将显著提升。

---

## 💬 纯对话模式 (Pure Chat)

v9.9.0 对纯对话体验进行了全面升级，它不再是一个临时的“草稿纸”，而是一个功能完备的 AI 助手工作台。

### 1. 开启方式
- **显式切换**: 在侧边栏下拉菜单中选择 **"💬 纯对话模式 (Pure Chat)"**。
- **隐式触发**: 在未选择任何知识库的情况下直接提问，系统会自动切换至纯对话模式并保存记录。

### 2. 多会话管理
现在，纯对话模式拥有与知识库完全一致的会话管理能力：
- **➕ 新建会话**: 点击侧边栏的“新建会话”按钮，开启一个全新的话题（例如：一个用于代码辅助，一个用于写周报）。
- **🕒 历史回溯**: 所有的纯对话记录都会根据您的账号进行**隔离存储**。您随时可以从侧边栏的历史列表中找回之前的对话。
- **功能对齐**: 支持会话重命名、置顶、删除以及 URL 分享。

---

## 📎 交互革命：全能附件 (Universal Attachment)

在主对话框中，我们合并了所有上传入口。

- **统一入口**: 点击 `+` 或使用拖拽功能，即可上传 **图片、PDF、Word、Excel、TXT** 甚至 **视频**。
- **自动 OCR**: 只要包含图片，系统将自动调用原生加速引擎进行文字打捞。
- **上下文继承**: 上传的附件会自动进入当前对话的“短期记忆”，您可以针对该文件直接提问（如：“总结这个表格的数据趋势”）。

---

## 🚀 万能追加：管理您的知识资产 (Omni-Append)

知识库不是一次性的，通过 **「🚀 增强追加」** 面板，您可以随时扩充资料：

### 1. 五大摄入源
支持与创建时完全一致的 5 种模式：文件上传、本地路径扫描、文本粘贴、网页抓取、数据库快照。

### 2. 构建逻辑约束 (重要)
- **追加模式下**: **「智能数据分析」** 选项会被强制隐藏。
    - *原因*: 数据分析需要对 SQL 数据库进行全量建模。若需开启分析功能，请在创建知识库时选择，或在追加时勾选 **「强制重索引 (Full Rebuild)」**。
- **增量 vs 全量**:
    - **默认 (不勾选重索引)**: 仅对新抓取的文件进行索引，速度极快，不影响旧数据。
    - **勾选重索引**: 先合并所有物料，然后彻底重置该知识库的向量索引。

---

## 🔗 会话持久化与分享

v9.5.38 引入了企业级的会话跟踪技术：

- **会话持久化**: 刷新页面不再丢失对话！系统会自动加载您在该知识库下的最后一次通话。
- **URL 分享**: 管理员可在“会话历史”中获取包含 `?session_token=...` 的链接。
    - 将此链接发给有权限的同事，对方可直接进入该对话上下文。
- **跨设备同步**: 只要 Token 未过期，您在电脑上未完成的对话，可以在移动端通过链接继续。

---

## 🛡️ 资源治理矩阵 (Resource Matrix)

(已合并至上文“全域治理与监控中心”章节)
