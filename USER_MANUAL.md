# RAG Pro Max v6.7.2 企业用户手册 (Strategic Intelligence Edition)

**版本**: v6.7.2  
**更新日期**: 2026-01-18  
**核心特性**: Strategic Workshop 3.0、精准语义选表、领域感知引擎

---

## 🧠 战略分析车间 3.0 (Strategic Workshop)

在 v6.7.2 中，数据分析引擎升级为**战略级智能化**，实现了“理解业务 -> 精准选表 -> 专项推演”的逻辑闭环。

### 1. 智能寻表 (Precise Identification)
系统不再盲目扫描全量表，而是通过 LLM 深度理解您的提问意图。
- **自动对齐**: 当您询问“各区域销售分布”时，系统会自动从 10+ 张表中精准选出具备销售和区域字段的核心表。
- **领域语义感知**: 系统能够识别专业的前缀逻辑（如：自动将 `mxck` 识别为出口业务，将 `mxxx` 识别为国内销项），无需用户在提问中解释字段含义。

### 2. 按需数据注入 (Targeted Generation)
针对只有表结构描述的知识库，系统执行“外科手术式”模拟。
- **专项模拟**: 系统只为回答当前问题必须用到的 1-3 张表注入模拟数据，大幅降低计算延迟。
- **逻辑自洽性**: 模拟数据将严格遵循字段的业务描述（Comment），确保金额、日期、企业名称等关键指标符合业务逻辑常理。

---

## 🛡️ 资源治理矩阵 (Unified Governance)

在 v6.7.0 中，我们重构了管理员控制台的资源管理功能，通过**归一化视图**提升治理效率。
*   **物理透视**: 实时扫描文件系统，直观展示知识库的 **“创建时间”** 与 **“最后修改时间”**，帮助管理员精准判定资产时效性。
*   **智能分类**: 系统根据元数据与命名模式自动标识库类型（📚 文档库 / 🌐 网页库 / 💬 对话库）。
*   **确定性批量操作**: 通过动态 Key 刷新技术，确保“✅ 全选列表”后的批量授权或删除操作 100% 成功。

---

## 📎 对话增强：万能附件 (Universal Attachment)

聊天框上方的上传区域已升级为**万能解析入口**，支持 20+ 种格式的即时注入。
*   **核心逻辑复用**: 附件解析直接调用系统核心管线，确保图片 OCR 质量与文档解析质量与知识库构建标准完全对齐。
*   **即时上下文**: 拖入文件后，内容将作为上下文自动注入当前对话，无需预先构建知识库。
*   **多图追加**: 支持连续上传多张图片，系统会自动追加识别文本而非覆盖。

---

## 🧠 智能数据分析 2.0 (Data Analyst Agent)

### 1. 构建即就绪 (Build-First Architecture)
数据分析引擎遵循“物理闭环”原则，将数据准备前置化。
- **物理底座固化**: 在点击“构建知识库”阶段，系统即完成 **“有数传数/无数造数”** 的全过程。
- **就绪声明**: 构建完成后，物理数据库 `business_data.db` 已处于包含真实或仿真数据的就绪状态，确保第一句提问即可获得实质性响应。

### 2. 按需加载与下钻分析
- **智能剪枝 (Pruning)**: 系统仅针对当前问题相关的表执行校验，显著提升海量表环境下的初始化速度。
- **跨阶段记忆 (Drill-Down)**: 支持基于前序分析发现的异常点（如特定 ID、趋势拐点）进行连续追问，系统自动继承 SQL 查询上下文。

---

## 🖼️ 终极资产包：镜像级现场还原 (Snapshot)

全量资产包 (ZIP) 旨在实现完全的“数据主权”，其结构如下：

1.  **`01_核心资产`**: 知识摘要报告与详细资产清单。
2.  **`02_历史对话 (镜像还原)`**: 
    *   **`MD_纪要/`**: 包含完整角色标识与 KPI 汇总的 Markdown 记录。
    *   **`Charts_IMG/`**: 所有的分析图表均保留 Retina 级 PNG 图片。
    *   **`Charts_HTML/`**: 供离线交互的 Plotly 网页图表。
3.  **`03_战略大脑`**: 包含沉淀的物理数据库 (`business_data.db`) 与业务蓝图 (`schema.json`)。
4.  **`04_系统配置`**: 包含 Manifest 元数据及处理参数。
5.  **`05_原始文档库`**: 包含您上传的所有原始附件。
6.  **`06_向量引擎快照`**: 供系统迁移使用的二进制索引。

---

## 🕷️ 网页抓取：全量饱和模式 (Saturation Mode)

针对大型文档站的深度抓取进行了专项增强。
*   **饱和式队列**: 只要链接属于设定的产品作用域，系统将全量搬运，不留死角。
*   **反爬避让**: 强制单线程配合随机抖动（Jitter）延迟，最大程度模拟人类访问行为。

---

## 🛡️ 安全与自愈 (Security & Self-Healing)

- **终端自愈**: 新增基于端口监测的服务自愈逻辑，支持强制释放僵尸进程。
- **权限固化**: 针对 macOS 环境自动应用权限补丁，解决多用户访问冲突。
- **隐私脱敏**: 导出记录自动过滤 API 密钥等敏感元数据。

---

## ❓ 常见问题 (FAQ)

**Q: 为什么数据分析构建变慢了？**
A: 因为 v6.7.0 将造数逻辑前置。这意味着您在对话阶段将获得“零等待”的 SQL 响应体验。

**Q: 如何确保仿真数据的准确性？**
A: 在上传的文档中对关键字段（如状态、分类）给出明确的枚举值定义，仿真引擎将自动捕获这些指纹。

---

**🎯 目标**: 为企业用户提供专业、高性能、高稳定的智能数据分析与 RAG 解决方案！
