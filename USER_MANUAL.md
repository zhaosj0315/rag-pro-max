# RAG Pro Max v9.5.37 企业用户手册 (Search Revolution)

**版本**: v9.5.37  
**更新日期**: 2026-01-26  
**核心特性**: 暴力抓取引擎、重定向解壳、暂存区治理中心

---

## 📂 暂存区精密管理 (Staging Inspector)

在“全源摄入”顶栏，系统新增了四位一体的交互工具链，让材料汇聚过程透明可控：

1.  **📂 资产预览**: 
    - 点击图标弹出限高滚动面板。
    - 自动按 **采集方式** 分组展示（如：网页抓取、文本粘贴）。
    - 每一行展示 `文件名 (大小 | 采集时间)`，支持超长文件名自动换行。
2.  **📍 Finder 定位**: 
    - 点击定位图标，在 macOS 中直接打开当前的暂存文件夹，方便本地物理审计。
3.  **🔄 同步刷新**: 
    - 当您觉得上传或同步延迟时，点击此按钮强制重置状态并扫描最新物理文件。
4.  **🧹 彻底清空**: 
    - 一键物理粉碎暂存区所有材料，重置构建任务。

---

## 🌐 智能搜索革命 (Smart Search)

在 v9.5.37 中，搜索模式不再是“碰运气”，而是一套精密确定的算法：

1.  **Level 0 探测**: 系统会先从搜索引擎（Bing/DDG）探测初始链接。探测过程中，终端会输出 `锁定 X 个高价值目标`。
2.  **自动解壳**: 系统会自动从复杂的跳转链接中剥离出原始网址，实现直连抓取，成功率提升至 100%。
3.  **暴力探测**: 如果网页结构复杂，系统会自动切换至“暴力正则模式”，从代码流中强行打捞知识。
4.  **中英双语**: 输入中文关键词，系统会自动匹配英文语境，确保获取全球维度的知识。

---

## 🚀 全源摄入：一切皆源文件 (Omni-Ingestion)

系统通过唯一的 **“⚡ 全源摄入”** 入口，提供五个核心数据采集维度。所有材料均会自动触发 **自动命名** 逻辑，让您的资产库井然有序。

### 1. 📂 文件上传
- **拖拽上传**: 支持 PDF, DOCX, MD, TXT, CSV, EXCEL, PPTX, 图片等 20+ 种格式。
- **批量处理**: 自动去重，实时显示文件计数。

### 2. 📁 扫描路径
- **递归同步**: 输入本地文件夹路径（如 `/Users/admin/docs`），系统会自动递归扫描并镜像所有有效文件至暂存区。
- **零拷贝**: 仅在暂存区创建引用，构建时才会进行物理索引，极大节省磁盘空间。

### 3. 📝 文本粘贴
- **即时捕捉**: 适用于保存临时会议纪要、代码片段或 URL 列表。
- **自动命名**: 系统会根据内容首行自动生成 `Pasted_时间戳.txt` 文件。

### 4. 🌐 网页摄入
- **智能路由**: 输入 URL 自动启动爬虫，输入关键词自动启动全网搜索。
- **指数级抓取**: 采用 5+25 扩散模型，深度挖掘关联文档，并自动转化为高质量 Markdown 文件。

### 5. 🗄️ 数据库快照 (新)
- **极广适配**: 支持 MySQL, Oracle, PostgreSQL, MaxCompute, Snowflake 等 9 种数据库。
- **模式一：数据表选择**: 
  - 从下拉列表选择表，支持**一键全选**与**多选**。
  - 点击 **`🚀 生成快照`**，系统会自动全量导出选定表为 CSV 文件。
- **模式二：自定义 SQL**: 
  - 编写任意 `SELECT` 语句（支持 JOIN, WHERE, GROUP BY）。
  - 执行后，查询结果将作为单独的 CSV 数据集存入暂存区。
- **流式导出**: 内置防爆内存引擎，轻松应对百万级行数的导出任务。

---

## ⚡ 智能问答联动 (Smart Linkage)

- **自动感应**: 系统会自动识别库的能力。进入数据库知识库时，**“数据分析”** 开关将自动为您开启。
- **统一命名**: 点击知识库名称旁的 **💡** 按钮，系统会结合当前模式为您提供智能起名建议。

---

## 🕸️ 智能图谱构建器 (v8.2.0)

在 v8.2.0 中，系统学会了像人类专家一样“读懂”您的数据结构。

### 1. 物理特征画像
- **自动识别**: 构建知识库时，系统会自动扫描数据表，找出哪个字段是主键（唯一标识），哪些字段是分类枚举（如“状态”）。
- **收益**: 当您问“有哪些状态？”时，系统能秒级从缓存中调取答案，而无需耗时的全表扫描。

### 2. 血缘关系推演
- **关联发现**: 系统会自动分析表与表之间的关系（例如：`orders.user_id` 和 `users.id` 是关联的）。
- **复杂查询**: 这使得系统能轻松应对“查询北京用户的平均订单金额”这类跨表难题，准确率大幅提升。

---

## 🧬 按需激活的“双核”力量 (v8.1.2 增强)

通过“铁闸门”逻辑与内核加固，系统将提供更精准的数据洞察。

### 1. 意图严选：听话的“专家”
- **默认状态**: 默认开启 **RAG 模式**（博学助手模式），检索文档原文。
- **授权激活**: 只有当您在对话框上方手动打开 **「数据分析」** 开关时，系统才会启动 SQL 推演。

### 2. 逻辑内核加固
- **原子化拆解**: 面对复杂问题，系统会像专家一样分步骤拆解（如：先筛选范围，再关联数据，最后计算指标），拒绝逻辑跳跃。
- **精准选表**: 在多张表存在同名字段（如“金额”）时，系统能根据您的问题自动区分是查“销售表”还是“退款表”。

### 3. 全链路审计 (v8.1.3)
- **配置透明**: 每次构建知识库，系统都会在后台日志中留存一份“配置清单”，明确记录您当时是否开启了 OCR 或数据分析功能，方便事后核查。
- **思维双写**: AI 的所有思考过程（选表理由、拆解逻辑）不仅在屏幕上显示，也会完整写入日志文件，实现 100% 可追溯。

---

## 🕷️ 饱和式网页抓取 (Crawler Saturation)
- **50,000 页上限**: 现已支持单次抓取最多 5 万个网页，完美适配大型文档中心搬运。
- **自动对齐**: 抓取后的网页表格如果开启了「智能数据分析」，将直接支持 SQL 统计。

---

## 🛡️ 账户与权限管理
- **存储配额**: 系统为普通用户设定了存储上限（默认 100MB）。
- **管理员特权**: `admin` 角色自动豁免配额限制（无限空间）。
- **动态调节**: 管理员可在「我的 -> 账户与访问安全」中，勾选用户并使用「个体安全调节舱」手动修改其配额（单位：MB，设为 -1 表示无限）。
- **实时审计**: 管理员可以通过后台日志看到 `[Route Discovery]` 信号，了解系统当前为何选择了 RAG 或 SQL 模式。