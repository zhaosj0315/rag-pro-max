# RAG Pro Max v6.6.5 企业用户手册 (Repair & Enhancement Edition)

**版本**: v6.6.5  
**更新日期**: 2026-01-16  
**核心特性**: 饱和爬取引擎、按需建表、表名自愈、全局悬浮导航

---

## 🎨 全局悬浮导航 (Floating Nav Capsule)

在 v6.6.5 中，我们为长页面（尤其是长对话流）引入了智能悬浮导航组。
*   **⬆️ Top**: 点击瞬间平滑滚动回页面最顶端（知识库 Header 区域）。
*   **⬇️ Bottom**: 点击瞬间直达页面最底部（对话输入框区域）。
*   **交互提示**: 按钮组位于屏幕右下角，采用半透明玻璃拟态设计，鼠标悬浮即可激活高亮。

---

## 🕷️ 网页抓取：全量饱和模式 (Saturation Mode)

针对阿里云帮助文档、Python 官方文档等大型、深层级站点，爬虫引擎进行了饱和式升级。
*   **饱和式队列**: 抛弃传统“分层递归”限制，改为“饱和式任务队列”。只要链接属于产品作用域，系统会一个不漏地进行全量搬运。
*   **智能识别**: 自动感应文档站根路径（如 `.../analyticdb-for-mysql/`），锁定抓取范围，防止抓取溢出。
*   **稳健抓取**: 强制单线程顺序爬取配合随机延迟，完美模拟人类阅读频率，有效规避 WAF 防火墙封锁。

---

## 🧠 智能数据分析 2.0 (Data Analyst Agent)

### 1. 按需精准加载 (On-Demand Schema)
系统现在具备**剪枝能力**。
*   **场景**: 如果您的知识库包含 100 张表，但您只问了一个关于“订单”的问题。
*   **优化**: 系统会自动锁定 `orders` 表，**仅对该表**执行状态校验。分析前摇时间从 30 秒缩短至 1 秒。

### 2. 表名自愈与映射 (Schema Healing)
系统不再死板匹配表名。
*   **自愈逻辑**: 如果大模型生成的 SQL 想查 `order`，而数据库里实际存的是 `orders`、`t_order` 或 `ORDER_DATA`。
*   **映射机制**: 系统会自动发现这些“相似实体”并建立内存映射，强制纠正 SQL 中的表名，确保查询成功率。

### 3. 数据持久化复用
*   **行数检查**: 系统在每次分析前都会检查物理表的数据量。只要表中已有数据（哪怕是历史仿真数据），系统将**永久复用**，绝不进行重复仿真。

---

## 🖼️ 镜像级资产包指南 (v6.2.2 更新)

v6.6.0 对数据分析引擎进行了**智能体化**重构，现在它不仅是一个 SQL 生成器，更是一位具备记忆与推理能力的虚拟分析师。

### 1. 下钻分析 (Drill-Down Analysis)
系统现在具备**跨阶段记忆能力**。
*   **场景**: 当您在第一轮分析中发现“异常用户 ID: 10086”时。
*   **智能行为**: 您可以直接追问“分析这个用户的行为路径”。系统会自动将 `ID: 10086` 注入下一阶段的 SQL 中 (`WHERE user_id = 10086`)，无需您重复输入上下文。

### 2. 分析记忆库 (Analysis Memory)
系统内置了**经验沉淀机制**。
*   **自动学习**: 每当系统成功执行一条复杂的 SQL 并获得有效数据，它会自动将这条逻辑“背诵”下来。
*   **举一反三**: 下次遇到类似问题（例如从“查东区销售”变成“查西区销售”），系统会调取记忆库中的高分模板，大幅提升代码准确率。

### 3. 可解释性报告 (Explainable SQL)
生成的 SQL 代码现在采用了**模块化结构 (CTE)** 并附带**白话文注释**。即使是非技术人员，也能通过阅读 SQL 中的注释（如 `-- 步骤1：筛选最近30天订单`）来验证分析逻辑的合理性。

---

## 🖼️ 镜像级资产包指南 (v6.2.2 更新)

现在生成的 **“终极六福资产包”** 具备完整的离线报告还原能力。导出的 ZIP 结构如下：

1.  **`01_核心资产`**: 知识摘要报告与资产清单。
2.  **`02_历史对话 (镜像还原)`**: 
    *   **`MD_纪要/`**: **[全量镜像]** 所有的对话记录均以 Markdown 格式还原，包含气泡标识、统计性能、KPI 汇总及内嵌图片。
    *   **`Charts_IMG/`**: 存放所有分析阶段的 PNG 图片，支持在 Markdown 中直接显示。
    *   **`Charts_HTML/`**: 存放所有交互式图表网页，供离线深度探索。
    *   **`Raw_JSON/`**: 原始对话序列化文件。
3.  **`03_战略大脑`**: 包含 **`business_data.db`** 真实沉淀数据库以及业务模型定义。
4.  **`04_系统配置`**: 包含 manifest 元数据。
5.  **`05_原始文档库`**: 包含您通过 5 种模式上传/抓取的所有原始文献原件。
6.  **`06_向量引擎快照`**: 底层向量索引备份。

---

## 🌍 中英双语业务发现 (Business Insight)

在每一个数据分析图表的上方，系统都会自动为您解读：
*   **格式**: `[中文结论] | [English Insight]`。
*   **用途**: 旨在帮助业务决策者在不看原始数据的情况下，瞬间抓住当前分析阶段的核心发现。

---

## 💾 现场恢复与会话持久化 (v5.6.8)

用户现在可以一键带走知识库的所有关联资产。生成的 ZIP 包结构如下：
1.  **`01_核心资产`**: 包含知识摘要与详细清单.
2.  **`02_历史对话`**: **[全量打捞]** 包含该知识库下的**所有**历史对话（MD 级还原 + JSON 原件）。
3.  **`03_战略大脑`**: 包含业务模型 (Schema) 与蓝图定义。
4.  **`04_系统配置`**: 包含底层处理元数据。
5.  **`05_原始文档库`**: 包含上传的所有原件（PDF/CSV/MD 等）。
6.  **`06_向量引擎快照`**: 供迁移使用的二进制存储。

---

## 💎 全域开模分析 (Universal Modeling)
系统支持 PDF/MD 字典与数据表的混合深度建模。只要文件名包含 `csv`, `excel`, `dict` 等关键字，或在上传时开启“数据分析”模式，系统即可自动识别并开模。

---

## 🛡️ 安全与合规

- **数据主权**: 知识库 ZIP 导出功能支持全量迁移原始文档、向量库及对话记录。
- **隐私保护**: 自动过滤日志中的敏感参数，支持 API 密钥环境变量隔离。
- **权限自愈**: 日志系统具备 `chmod 666` 强固化能力，彻底解决 macOS 多用户访问权限冲突。

---

## ❓ 常见问题 (FAQ)

**Q: 刷新页面后，知识库挂载失败怎么办？**
A: v5.6.8 引入了路径自愈引擎，系统会自动尝试模糊匹配前缀。若仍失败，请检查 `vector_db_storage` 目录下文件夹名是否包含非法特殊字符。

**Q: 为什么我只能看到最后一段聊天记录？**
A: 请确保使用“全量资产包”导出。普通界面的“导出”按钮仅针对当前显示的会话。

---

**🎯 目标**: 为企业用户提供专业、高性能、高稳定的智能数据分析与 RAG 解决方案！