# RAG Pro Max v9.2.0 企业用户手册 (Flagship Evolution)

**版本**: v9.2.0  
**更新日期**: 2026-01-24  
**核心特性**: 全域局部刷新、零白屏交互、Finder 深度集成

---

## 🚀 极致体验：零白屏、无缝交互

在 v9.2.0 中，系统交互性能再次跃升，彻底根除了页面级重载带来的白屏感：

1. **瞬时配置切换 (Zero Flicker)**
   - 顶部的 **联网搜索、智能研究、数据分析** 等功能开关，以及侧边栏的高级选项，现在点击后 **瞬时切换**。
   - 所有的状态更新均在局部完成，彻底消除了旧版本中的全页白屏闪烁。

2. **Gemini 级对话体验 (Direct Reply)**
   - **即点即答**: 点击底部的“推荐问题”或历史记录中的“引用此回复”，系统会 **直接** 在当前对话流中生成回答。
   - **无缝衔接**: 移除了中间态的页面刷新，操作响应延迟降至 0ms，体验如原生应用般丝滑。

3. **纯对话模式 (Pure Chat+)**
   - **彻底解耦**: 该模式不再依赖任何底层知识库文件，加载速度提升 100%。
   - **极致流式**: 优化了大模型直连通道，确保在各种网络环境下都能实现稳定、顺滑的逐字输出。

---

## 📁 知识库深度管理 (File Intelligence)

在“知识库详情与管理”面板中，系统提供了工业级的文件深度洞察能力：

1. **深度档案与数据取证**
   - 点击文件列表中的“🔍”图标，即可展开深度档案面板。
   - **健康仪表盘**: 顶部 4 枚实时徽章展示索引状态、存储效率、热度及存活天数。
   - **取证级属性**: 查看 Magic Bytes、SHA-256 指纹、文件系统类型及 RAG Token 预估。

2. **跨平台文件管理**
   - **📂 在 Finder 中显示**: 点击即可在 macOS Finder 中直接定位源文件。若文件已被清理，系统会给出明确的路径提示。
   - **👁️ QuickLook**: 调用 macOS 系统级预览引擎，支持 PDF、图片、代码文件的原生快速查看。
   - **无感交互**: 文件列表的翻页、搜索及上述操作均在 **局部刷新** 中完成，不会导致页面闪烁或重载。

---

## 🚀 数据摄入：三大全能入口

系统通过深度整合，提供三个“一站式”资料摄入入口，覆盖本地、互联网与企业系统数据。

1. **📂 文件上传 (静态数据全能表单)**
   - **无感采集**: 在上传文件、添加路径或粘贴文本时，侧边栏会实时更新 `📦 待处理暂存区` 计数，整个收集过程 **不闪屏、不重载**。
   - **上传**: 点击或拖入文件，系统即时搬运至暂存区。
   - **路径**: 输入本地路径后点击 **`➕ 添加`**，执行递归同步。
   - **粘贴**: 点击 **`📝 粘贴...`** 展开面板，内容实时持久化。
   - **暂存管理**: 收集齐材料后，点击底部的 **`🚀 立即创建`** 即可开始构建。

2. **🌐 互联网提取 (动态网络智能入口)**
   - **智能路由**: 根据输入内容自动识别 URL（爬虫）或 关键词（搜索）。
   - **指数级抓取 (5+25)**: 采用 Level 0 种子隔离算法，输入页数 $n$，系统将精准抓取 $1 + n + n^2$ 个深度关联文档。

3. **🔌 数据库同步 (结构化业务入口)**
   - **极广适配**: 原生支持 MySQL, Oracle, PostgreSQL, MaxCompute, Snowflake 等 9 种数据库。
   - **即时预览**: 选表时点击 **`👁️`** 图标，即可在气泡中即时预览数据，无需切换页面。

---

## 🔌 企业级全源数据库集成 (v8.6.9)

在 v8.6.9 中，系统实现了对 9 种主流数据库的全面支持，支持从本地到云端、从常规库到大数据引擎的全量接入。

### 1. 极广适配列表
- **MySQL / PostgreSQL / SQL Server / Oracle**: 企业核心业务库。
- **SQLite / DuckDB**: 本地数据文件与轻量分析。
- **ClickHouse**: 高性能 OLAP 分析。
- **MaxCompute (DataWorks) / Snowflake**: 云原生大数据平台。

### 2. 配置与深度预览
- **入口**: **「🔐 资源治理」** -> **「🔌 数据源连接」**。
- **四维透视**: 点击 **「🔍 预览」** 可查看字段定义、50 行数据采样、外键关系及业务统计信息。

### 3. 一键同步构建 (侧边栏革命)
- **入口**: 侧边栏选择 **「🔌 数据库同步」**。
- **即时预览**: 选表时点击 **`👁️`** 即可在气泡中即时预览数据，无需切换页面。
- **全选同步**: 点击“全选所有表”并输入知识库名称，点击底部的 **`🚀 立即创建`** 即可开始。系统会自动完成镜像拉取与知识库构建。

---

## ⚡ 智能问答联动 (Smart Linkage)

- **自动感应**: 系统会自动识别库的能力。进入数据库知识库时，**“数据分析”** 开关将自动为您开启。
- **统一命名**: 点击知识库名称旁的 **💡** 按钮，系统会结合当前模式为您提供智能起名建议。

---

## 🕸️ 智能图谱构建器 (v8.2.0)

在 v8.2.0 中，系统学会了像人类专家一样“读懂”您的数据结构。

### 1. 物理特征画像
- **自动识别**: 构建知识库时，系统会自动扫描数据表，找出哪个字段是主键（唯一标识），哪些字段是分类枚举（如“状态”）。
- **收益**: 当您问“有哪些状态？”时，系统能秒级从缓存中调取答案，而无需耗时的全表扫描。

### 2. 血缘关系推演
- **关联发现**: 系统会自动分析表与表之间的关系（例如：`orders.user_id` 和 `users.id` 是关联的）。
- **复杂查询**: 这使得系统能轻松应对“查询北京用户的平均订单金额”这类跨表难题，准确率大幅提升。

---

## 🧬 按需激活的“双核”力量 (v8.1.2 增强)

通过“铁闸门”逻辑与内核加固，系统将提供更精准的数据洞察。

### 1. 意图严选：听话的“专家”
- **默认状态**: 默认开启 **RAG 模式**（博学助手模式），检索文档原文。
- **授权激活**: 只有当您在对话框上方手动打开 **「数据分析」** 开关时，系统才会启动 SQL 推演。

### 2. 逻辑内核加固
- **原子化拆解**: 面对复杂问题，系统会像专家一样分步骤拆解（如：先筛选范围，再关联数据，最后计算指标），拒绝逻辑跳跃。
- **精准选表**: 在多张表存在同名字段（如“金额”）时，系统能根据您的问题自动区分是查“销售表”还是“退款表”。

### 3. 全链路审计 (v8.1.3)
- **配置透明**: 每次构建知识库，系统都会在后台日志中留存一份“配置清单”，明确记录您当时是否开启了 OCR 或数据分析功能，方便事后核查。
- **思维双写**: AI 的所有思考过程（选表理由、拆解逻辑）不仅在屏幕上显示，也会完整写入日志文件，实现 100% 可追溯。

---

## 🕷️ 饱和式网页抓取 (Crawler Saturation)
- **50,000 页上限**: 现已支持单次抓取最多 5 万个网页，完美适配大型文档中心搬运。
- **自动对齐**: 抓取后的网页表格如果开启了「智能数据分析」，将直接支持 SQL 统计。

---

## 🛡️ 账户与权限管理
- **存储配额**: 系统为普通用户设定了存储上限（默认 100MB）。
- **管理员特权**: `admin` 角色自动豁免配额限制（无限空间）。
- **动态调节**: 管理员可在「我的 -> 账户与访问安全」中，勾选用户并使用「个体安全调节舱」手动修改其配额（单位：MB，设为 -1 表示无限）。
- **实时审计**: 管理员可以通过后台日志看到 `[Route Discovery]` 信号，了解系统当前为何选择了 RAG 或 SQL 模式。