# RAG Pro Max v5.6.8 企业用户手册 (Production Stability Edition)

**版本**: v5.6.8  
**更新日期**: 2026-01-13  
**核心特性**: 持久化会话现场、路径自愈引擎、高保真技术文档爬虫、权限自愈日志系统

---

## 📚 核心功能详解 (v5.6.8 更新)

### 💾 现场恢复与会话持久化 (Persistent Memory)
用户现在可以像保存书签一样保存对话状态：
- **自动恢复**: 刷新页面或重新打开浏览器，系统会从 URL 参数中自动识别并加载上一次活跃的知识库和对话。
- **书签支持**: 您可以直接将带有 `?kb_id=...&sess_id=...` 的地址保存为收藏，点击即可秒回特定的讨论现场。
- **秒级同步**: 侧边栏标题现在与对话内容实时同步，不再受异步落盘延迟影响。

### 🕷️ 高保真网页爬虫 (Advanced Web Crawler)
针对大型技术文档（如阿里云帮助中心）进行了深度优化：
- **精准提取**: 默认优先抓取 `.content-wrapper` 核心区域，自动剥离侧边栏导航、页眉页脚等干扰。
- **参数推荐**: 抓取大型文档库时，建议设置 **爬取深度 >= 3**，**最大页数 >= 50**。
- **格式保护**: 采用 `html2text` 引擎，即便在 Markdown 转换中也能完美保留超长代码行和复杂表格。

### 📜 实时终端日志查看 (Log Viewer)
无需切换到终端窗口，即可在 Web UI 监控系统运行状态：
- **入口**: 侧边栏【📊 监控】 -> 【📋 终端日志】子标签。
- **性能**: 针对大容量 `.jsonl` 日志进行了流式优化，仅读取文件末尾关键信息，避免系统假死。
- **权限**: 内置权限自愈逻辑，支持多用户/Root 混合运行环境下的日志穿透读取。

---

## 📦 资产全量下载 (Full Asset Pack)
用户现在可以一键带走知识库的所有关联资产。生成的 ZIP 包结构如下：
1.  **`01_核心资产`**: 包含知识摘要与详细清单.
2.  **`02_历史对话`**: **[全量打捞]** 包含该知识库下的**所有**历史对话（MD 级还原 + JSON 原件）。
3.  **`03_战略大脑`**: 包含业务模型 (Schema) 与蓝图定义。
4.  **`04_系统配置`**: 包含底层处理元数据。
5.  **`05_原始文档库`**: 包含上传的所有原件（PDF/CSV/MD 等）。
6.  **`06_向量引擎快照`**: 供迁移使用的二进制存储。

---

## 💎 全域开模分析 (Universal Modeling)
系统支持 PDF/MD 字典与数据表的混合深度建模。只要文件名包含 `csv`, `excel`, `dict` 等关键字，或在上传时开启“数据分析”模式，系统即可自动识别并开模。

---

## 🛡️ 安全与合规

- **数据主权**: 知识库 ZIP 导出功能支持全量迁移原始文档、向量库及对话记录。
- **隐私保护**: 自动过滤日志中的敏感参数，支持 API 密钥环境变量隔离。
- **权限自愈**: 日志系统具备 `chmod 666` 强固化能力，彻底解决 macOS 多用户访问权限冲突。

---

## ❓ 常见问题 (FAQ)

**Q: 刷新页面后，知识库挂载失败怎么办？**
A: v5.6.8 引入了路径自愈引擎，系统会自动尝试模糊匹配前缀。若仍失败，请检查 `vector_db_storage` 目录下文件夹名是否包含非法特殊字符。

**Q: 为什么我只能看到最后一段聊天记录？**
A: 请确保使用“全量资产包”导出。普通界面的“导出”按钮仅针对当前显示的会话。

---

**🎯 目标**: 为企业用户提供专业、高性能、高稳定的智能数据分析与 RAG 解决方案！