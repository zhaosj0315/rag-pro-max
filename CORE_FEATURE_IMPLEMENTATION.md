# RAG Pro Max 核心功能实现详述 (Core Feature Implementation)

**版本**: v1.0.0
**状态**: 关键性资产 (永久保存)
**描述**: 本文档记录了系统五大核心功能的底层实现逻辑、业务架构及技术细节。

---

## 1. 📂 文件上传 (File Upload)
实现“构建即就绪”的高性能 RAG 索引管线。

- **扫描与预处理**：递归扫描上传目录，利用文件哈希（SHA-256）建立增量更新机制，避免重复处理。
- **并发解析 (Parallel Processing)**：
  - **CPU 密集型 (PDF/Docx)**：使用 `ProcessPoolExecutor` 多进程加速文本提取。
  - **I/O 密集型 (API 调用)**：使用线程池处理 OCR 或元数据注入。
- **嵌入与入库**：基于 `LlamaIndex` 框架，动态选择嵌入模型（BGE/MiniLM），构建 `VectorStoreIndex`。
- **元数据绑定**：在构建时自动注入 `file_path`, `creation_date` 等元数据，支持后续的精准溯源。

## 2. 📝 粘贴文本 (Paste Text)
轻量化、无盘化的即时对话能力。

- **虚拟文件映射**：将用户粘贴的文本内容映射为内存中的 `Document` 对象。
- **即时索引挂载**：在 `st.session_state` 中维护一个临时的 `SummaryIndex`，无需物理落盘即可实现跨段落查询。
- **动态清理**：会话结束后自动销毁内存中的向量空间，确保隐私与系统资源利用。

## 3. 🔗 网址抓取 (Web Crawl)
饱和式、防封控的内容抓取引擎。

- **饱和式抓取管线**：采用基于 Full URL Scope 的队列逻辑。只要发现符合前缀的链接，立刻入队，直至队列耗尽。
- **智能降速与 WAF 绕过**：并发限制固定为 1，引入随机延迟（Jitter），确保在不触发防火墙的情况下完成深度抓取。
- **内容脱水 (De-noising)**：利用 `html2text` 提取正文，并针对特定 `div` 容器（如 content-wrapper）进行锁定。

## 4. 🔍 智能搜索 (Keyword Search)
基于多源聚合与实时 RAG 的证据挖掘系统。

- **多引擎聚合**：支持 Google、Baidu、DuckDuckGo 等主流搜索接口的并发调用。
- **实时内容总结**：搜索结果返回后，系统实时抓取前 N 个结果的网页摘要，并利用大模型进行多维证据对比分析。
- **置信度评分**：根据域名权重、内容时效性、关键词匹配密度为每个搜索结果打分，并在回答中显式标注来源。

## 5. 📊 数据分析 (Data Analysis)
前置构建、物理闭环的 Text-to-SQL 决策工作站。

- **前置构建架构 (Build-First)**：
  - **有数传数**：解析 CSV/Excel 期间，通过 `pandas` 将数据物理化为 SQLite 表。
  - **无数造数**：从文档提取业务 Schema，AI 自动生成符合逻辑的仿真数据注入物理库。
- **执行链路**：
  1. **任务拆解**：将复杂查询拆分为 2-3 个 SQL 执行阶段。
  2. **数据指纹采样**：从物理库获取列值样例，消除 SQL 生成时的幻觉。
  3. **可视化渲染**：SQL 执行结果安全化处理（处理 NumPy/NaN），支持 Chart.js 或 Streamlit 原生图表持久化显示。
- **结论输出**：采用 SCQA 架构，基于真实执行结果（Row Count > 0）流式输出分析报告。

---

## 📑 管理规范
- **修改限制**：本文档属于核心业务逻辑定义，任何对底层实现的修改必须同步更新本文档。
- **永久保存**：禁止在清理脚本中包含此文档。
